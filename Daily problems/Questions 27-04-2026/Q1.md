### 1.

Take two numbers as string input, convert them to integers, and perform division. Handle invalid number format and division by zero.

---

### 2.

Create an array and allow the user to:

* Enter elements
* Choose an index
* Enter a divisor

Perform division using the selected array element. Handle invalid index and arithmetic errors.

---

### 3.

Take a string and two indices (start and end). Extract and print the substring. Handle invalid index ranges.

---

### 4.

Write a program that simulates opening and closing a resource. Ensure the closing statement always executes regardless of exceptions.

---

### 5.

Write a program to accept marks from the user. If marks are less than 0 or greater than 100, throw an exception. Otherwise, print the grade.

---

### 6.

Take a string input from the user:

* Convert it to an integer
* Access a character at a given index

Handle both invalid number format and string index errors.

---

### 7.

Write a program using nested try-catch blocks where:

* Inner block handles array index errors
* Outer block handles arithmetic errors
